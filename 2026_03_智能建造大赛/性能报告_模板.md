# 性能评估报告模板 · 智能建造赛题7

> 交付物③(官方要求):完整 Precision-Recall 曲线 + 各类目标检测精度 + **指定边缘端真实推理 FPS(≥30FPS)**。
> 这是 30FPS 硬门槛**唯一可信的证明**——务必报"含画框+编码"的端到端 FPS,而非裸推理。数据由 `eval.py` / `trt_infer_orin.py` 产出后填入。

## 1. 实验环境
| 项 | 配置 |
|---|---|
| 训练 | RTX4070S, ultralytics ___, torch ___, imgsz 1024 |
| 端侧 | Jetson Orin Nano 8G, JetPack 6.2, TensorRT ___, **Super Mode(MAXN_SUPER 25W)+jetson_clocks**, DC 供电 |
| 数据 | SeaDronesSee+AFO(公开)/ 命题方二开数据(___张, 训/验/测 ___) |

## 2. 检测精度(各类 PR 曲线 + mAP)
| 类别 | AP@0.5 | AP@0.5:0.95 | 备注 |
|---|---|---|---|
| 落水人员 | | | 救援核心,recall 优先 |
| 船只 | | | |
| 浮标 | | | |
| **mAP** | | | |
- [ ] 附各类 Precision-Recall 曲线图。
- [ ] 按目标像素面积分桶(tiny/small/medium/large)的召回表(`eval.py` 分桶召回)——证明小目标不漏。

## 3. 边缘端实时性(30FPS 硬门槛证明)
> 报**端到端三档**(trt_infer_orin.py 输出),以「③含编码」为准。Orin Nano 无硬件NVENC,软编码H265已计入。

| 档位 | imgsz | 精度模式 | 时延ms | FPS | vs 30 |
|---|---|---|---|---|---|
| ① 裸推理 | 640 | INT8 | | | |
| ② 含后处理(解码+NMS+画框) | 640 | INT8 | | | |
| ③ **含编码(实战)** | 640 | INT8 | | | ✅/❌ |
- [ ] FP16 vs INT8 × 640 vs 768 的 **FPS-召回帕累托曲线**(选最优工作点)。
- [ ] tegrastats 记录:功耗(W)、显存(MB)、nvpmodel 档位。

## 4. 消融实验(技术创新性的硬证据,逐项独立 A/B)
| 配置 | mAP | small召回 | 端侧FPS | 结论 |
|---|---|---|---|---|
| baseline (yolo12n) | | | | |
| + P2 头 | | | | 小目标↑ |
| + NWD 标签分配 | | | | |
| + Wise-IoU | | | | |
| + 水面物理增广 + GT-Glint 难负样本 | | | | 反光误报↓ |
| 去 P5(四头→三头) | | | | 帧率↑/精度? |
| + INT8 量化 | | | | 速度↑/掉点? |
- 每行只改一项,其余固定;禁止一次叠多项凭感觉。

## 5. 系统闭环验证
- [ ] 检测框→GPS:用已知 GPS 浮标做真值校验,报**投影误差区间**(诚实,几十米级)。
- [ ] QGroundControl 融合 demo:录屏 + 现场实时双轨。
- [ ] 时序滤波前后误报对比(反光闪点抑制率)。

## 6. 结论
- 硬指标:端到端含编码 ___ FPS ≥ 30 ✅;mAP ___;小目标召回 ___。
- 最优工作点:imgsz ___ + ___ 精度模式。
